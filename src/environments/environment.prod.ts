export const environment = {
  production: true,
  unsplashApiKey: '',
};

// PRODUCTION SECURITY:
// API keys should NEVER be in production frontend code.
// Create a backend API that proxies requests to Unsplash.
